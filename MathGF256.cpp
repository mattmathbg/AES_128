//
// Created by mmath on 20/03/2026.
//

#include "MathGF256.h"#include "MathGF256.h"

uint8_t xtime(uint8_t x) {
    // TODO: Implémenter le décalage binaire vers la gauche (x << 1).
    // TODO: Si le bit de poids fort (0x80) de 'x' était à 1 avant le décalage,
    // faire un XOR du résultat avec 0x1B.
    return 0;
}

uint8_t multiply(uint8_t x, uint8_t y) {
    // TODO: Implémenter la multiplication complète dans GF(2^8).
    // Astuce : Utilise une boucle (8 itérations) avec xtime() et des opérations XOR.
    return 0;
}